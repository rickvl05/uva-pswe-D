name: GUT-tests

on:
  push:
    branches:
      - 'rick/unit_tests'


jobs:
  test_1:
    name: "Test Action with Container"
    runs-on: ubuntu-latest
    steps:
    - name: Checkout
      uses: actions/checkout@v2
    - name: Run Action
      uses: manleydev/run-gut-tests-action@v1.1.0
      with:
        directory: PuzzlePlatformer